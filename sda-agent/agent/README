Please build LandingPage first. 
(Also the Agent only run once for depositing right now. Later I will develop it)

Step 1:

Modify sead2/sda-agent/config/config.properties file

need to get correct information for sftp.host, sftp.user, sftp.pass, collection.user and
collection.pass

write a proper path for 'dummySDA' that could save the temporary RO files

change 'LandingPage' to the correct http address

if you don't want to use SDA, you can create a local SDA folder and change ‘SDA’ path.

Step 2:

modify src/main/java/org/sead/SDA_Agent/Service/ServiceLauncher.java

make correct 'propertyPath' to sead2/sda-agent/config/config.properties


Step 3:

run build-standalone.sh.

./build-standalone.sh.

It will help user build project and create a shell script bin/SDA_Agent.sh


Step 4:

run bin/SDA_Agent.sh to start SDA Agent

nohup ./bin/SDA_Agent.sh > log.txt &

